# 整體專案功能測試單

**用途:** 上線前(或每次重大改動後)的完整人工複查清單。
**標記說明:** ✅=本次複查已用程式自動化實測過(見 SECURITY_REVIEW.md 附表);
☐=邏輯已寫且經 build/型別檢查,但建議正式上線前再用真實瀏覽器過一次人工 QA。
兩者都列出來,是為了不讓「已測過」跟「還沒測過」混在一起看不出差別。

**測試帳號:**
- 管理員:`admin@nccu.edu.tw`
- 示範教授(已認領):`professor.demo@nccu.edu.tw`
- 學生:任意 `@g.nccu.edu.tw` 信箱皆可(自動建立新帳號)
- 開發模式驗證碼直接顯示於登入頁畫面,無需真實信箱

---

## 一、身分驗證與帳號安全

- ✅ 非白名單網域信箱(非 `@nccu.edu.tw`/`@g.nccu.edu.tw`)無法登入
- ✅ 同一信箱 60 秒內重複請求驗證碼被擋
- ✅ 同一信箱 10 分鐘內超過 5 次請求驗證碼被擋且寫入安全事件
- ✅ 同一 IP 10 分鐘內超過 20 次跨信箱請求被擋
- ☐ 連續輸入錯誤驗證碼 5 次後帳號鎖定 15 分鐘(邏輯已寫,建議人工過一次實際登入畫面體驗)
- ✅ Session token 只存雜湊,資料庫查不到明文
- ☐ 閒置 7 天以上的 session 自動失效(邏輯已寫,需等待才能人工驗證,可改資料庫時間戳測試)
- ✅ 管理員帳號未設定 2FA 時,存取 `/admin` 系列頁面會被導向設定頁
- ✅ 管理員 2FA 已設定但超過 30 分鐘重驗時效,會被導向 step-up 重新驗證頁

## 二、目錄瀏覽與教授檔案

- ☐ 首頁 → 學院 → 系所 → 領域 → 子領域逐層瀏覽路徑正確(13 學院、77 系所)
- ☐ 教授卡片資訊分層顯示正確(專長標籤、是否開放媒合)
- ☐ 「為你精選」區塊正常顯示(已知為非個人化過渡版本,顯示合理即可)
- ✅ 反爬取:短時間內大量存取不同教授頁觸發漸進延遲/人機驗證,正常瀏覽不誤觸發

## 三、需求發布與申請

- ☐ 教授發布五類需求(TA/DEPT/UR/REC/IND)表單正確
- ☐ 需求列表前端即時篩選(免網路來回)
- ☐ 學生三步驟結構化申請表單(背景興趣→時間技能→確認送出)
- ✅ 對同一需求重複申請被唯一索引擋下
- ☐ 教授端並排比較申請頁正確顯示
- ✅ 申請人自己無法變更自己申請的狀態(僅該需求教授本人/管理員可以)
- ☐ 「我的申請」進度可視化狀態列正確反映實際狀態
- ✅ 狀態變更時 `statusUpdatedAt` 正確寫入(供學期報告統計使用)

## 四、站內訊息與聯絡方式

- ✅ 申請送出後自動建立對應對話(學生↔教授)
- ☐ 教授可直接發起對教授的合作邀請對話
- ✅ 每帳號每日發起新對話數上限正確生效
- ☐ 媒合確認(如邀請面試)後解除當次對話的頻率上限限制
- ☐ 忙碌/有空狀態雙方各自可手動切換,對方端正確顯示
- ✅ 聯絡方式新增後,資料庫原始值加密、不含明碼
- ✅ 聯絡方式揭露後,對方在該對話中可見且正確解密
- ☐ 未揭露的聯絡方式,對方看不到(人工在瀏覽器分別用兩個帳號複查)

## 五、檢舉與管理後台

- ☐ 檢舉表單送出成功,48hr/7日分級時限顯示正確
- ☐ 管理後台審核教授(核准/駁回)正確運作且通知教授
- ☐ 管理後台結案檢舉後,檢舉人與被檢舉對象皆收到中性通知(非無聲移除)
- ✅ `resolvedAt` 正確寫入(供學期報告平均處理時效統計使用)

## 六、稽核與雙人核可

- ✅ 稽核鏈遭竄改可被精準偵測到斷鏈位置
- ✅ 管理員無法核可自己提出的敏感調閱申請
- ✅ 另一位管理員核可後,申請人可在 30 分鐘時效窗口內存取
- ☐ 超過 30 分鐘時效窗口後,存取應被重新要求走核可流程(邏輯已寫,建議人工用改資料庫時間戳測試)
- ☐ `/admin/security` 頁面正確顯示安全事件列表
- ☐ `/admin/approvals` 頁面正確顯示待核可/我的申請清單

## 七、檔案上傳(§6)

- ✅ 偽造副檔名(可執行檔偽裝成 .pdf)被 magic number 正確擋下(415)
- ✅ 路徑穿越檔名不影響實際儲存路徑(儲存為隨機 UUID 檔名)
- ✅ 超過大小上限(10MB)的檔案被擋下(413)
- ✅ 未登入無法上傳(401)
- ✅ 每小時 20 次配額在高併發下精準生效,無孤兒檔案殘留
- ☐ 合法 Word 文件(.docx)上傳成功(本輪僅實測 PDF 路徑,Word 走同一套 magic number 邏輯但建議補測一次)
- ☐ 檔案下載連結(`/api/files/download/[token]`)僅申請人本人/該需求教授/管理員可用

## 八、帳號生命週期自動化

- ✅ 標記畢業偵測後,緩衝期內帳號權限不受影響(仍為 ACTIVE)
- ✅ 緩衝期到期後,批次處理正確將帳號轉為 ALUM(唯讀)
- ✅ 誤判的緩衝期可被管理員取消
- ✅ 休學(SUSPENDED)/復學(恢復 ACTIVE)/退學封存(ARCHIVED)三種狀態轉換正確
- ✅ 對非休學中帳號執行復學動作正確拋錯
- ✅ 教授交接天數限制(30–90 天)正確擋下超出範圍的申請
- ✅ 教授交接申請成功後,利益相關人(未結案申請人)收到通知
- ✅ 交接日到期後,該教授開放需求全數自動關閉(非刪除,凍結封存)
- ✅ 帳號血緣建立後,新舊帳號互相查得到關聯,且舊帳號資料完全未被動過
- ✅ 唯讀狀態帳號(ALUM/SUSPENDED/ARCHIVED)無法執行寫入類動作(申請/發訊息/發文,程式碼審查逐一確認)
- ☐ 建議正式上線前,用瀏覽器實際登入一個 ALUM 測試帳號,親自點一次「送出申請」按鈕確認前端也正確擋下並顯示提示文字(本輪因 Next.js Server Action 的 HTTP 呼叫機制限制,僅完成程式碼層級驗證,未做端到端瀏覽器操作)

## 九、面試時段預約 + ics 行事曆同步

- ✅ 10 人併發搶同一時段,精準只有 1 人預約成功,資料庫覆核一致
- ✅ 未預約的時段不會回傳地點欄位(媒合前不揭露)
- ✅ 已預約的時段不再出現在開放列表中
- ✅ ics 訂閱 token 核發、解析、偽造 token 正確被拒(404)
- ✅ ics 內容含正確的 VCALENDAR/VEVENT 格式,且不外洩使用者 email
- ☐ 訂閱連結貼到真實行事曆軟體(Google Calendar/Outlook)實際能訂閱顯示(本輪僅驗證產出的 ics 文字格式正確,未實測第三方軟體相容性)

## 十、教授群組

- ✅ 建立群組時,建立者自動成為成員(owner)
- ✅ 依信箱邀請已存在帳號成功;邀請不存在的信箱正確返回失敗且不留殘留使用者
- ✅ 群組貼文建立與查詢正常
- ✅ 非成員無法存取群組頁(HTTP 層 307 導回,寫入安全事件)
- ☐ 群組貼文不會出現在任何公開頁面或搜尋結果中(架構紅線,建議人工用未登入身分確認)

## 十一、AI 輔助

- ✅ 未設定 API 金鑰時,退回啟發式演算法且功能正常運作(非壞掉或回傳假資料)
- ✅ 標籤建議只會從候選子領域清單內挑選,不會發明新標籤
- ✅ 申請動機摘要非空、短於原文;空輸入不產生幻覺內容
- ☐ 若正式設定 `ANTHROPIC_API_KEY`,建議實際跑一次確認 live 模式輸出品質(本輪僅測 heuristic 退回路徑,因環境未設金鑰)
- ✅ 教授需手動確認才會真正套用 AI 建議的標籤(不自動寫入 `professorSpecialties`)

## 十二、學期聚合報告

- ✅ 分組樣本數精準邊界測試:4 筆隱藏顯示「＜5」,滿 5 筆起顯示實際數字
- ✅ 媒合成功率、平均處理時效等衍生統計,樣本不足時正確顯示「樣本不足,不呈現」而非誤導性數字
- ☐ `/admin/semester-report` 頁面的日期區間選擇器實際操作體驗(邏輯已測,UI 互動建議人工過一次)

## 十三、全站基礎設施

- ✅ CSP / HSTS / X-Content-Type-Options / X-Frame-Options / Referrer-Policy / Permissions-Policy 六項安全標頭實際生效(curl 覆核)
- ✅ `npm run build` 全部 34 個路由編譯成功、無型別錯誤
- ✅ drizzle-orm 0.45.2 升級後無 schema 差異、核心流程回歸測試通過
- ☐ 響應式版面在手機瀏覽器實際操作(本輪未做視覺/RWD 測試,僅功能邏輯測試)
- ☐ 中文/特殊字元(如表情符號)在各表單輸入欄位的顯示與儲存正確性

---

## 附註:關於「✅」的誠實範圍

標記 ✅ 的項目,代表**本次複查有實際寫程式碼呼叫對應函式或發送真實 HTTP 請求驗證過**,
不是「讀過程式碼覺得應該沒問題」。標記 ☐ 的項目**不代表功能有問題**,只是本輪測試方式
(直接呼叫 repository 函式、curl 打 API)沒有涵蓋到需要真實瀏覽器互動或視覺呈現的部分,
建議正式上線前再過一輪人工瀏覽器 QA。詳細的自動化測試方法與結果證據見 `SECURITY_REVIEW.md`。
