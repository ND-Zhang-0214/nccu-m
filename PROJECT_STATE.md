# PROJECT_STATE.md — 政大研究媒合平台專案狀態總覽

> **這份文件的用途:** 讓任何人(或任何一個新開的 AI 對話)在不重讀整個對話歷史的情況下,完整理解這個專案目前做到哪裡、為什麼這樣設計、還缺什麼。結構刻意做成人類跟 AI 都好讀——標題分明、決策都附理由、現況與待辦明確分開,不夾雜過程敘事。
>
> **給接手的 AI 模型看的第一句話:** 這是一個真實在開發中的專案,不是範例或練習。所有「已完成」項目都經過實際建置(`npm run build`)與功能驗證,不是只有程式碼存在。所有「未完成」項目都明確列出,不要假設它們已經做了。修改前先讀對應章節,不要整個專案重新設計。
>
> **最後更新:** 對應 git commit `ea58ddf`(feat(security) 那次)。之後有新進度應更新本檔對應章節,而不是另開一份新文件。

---

## 一、專案是什麼

**一句話:** 政大校內限定的研究媒合基礎設施——讓教授的研究助理/大專生計畫/推薦信/產學合作等需求,與學生在有身分驗證、有隱私保護、有存證機制的環境中對接。不是求職網站,不是社群平台,不收費、不放廣告。

**發起人背景:** 政大三年級學生(英文系+全球經濟治理雙主修),有學生自治經驗與 TWSE ESG 審查實務經驗。目標是先做出雛型,爭取校方資源(SSO 對接、學籍系統介接、校內主機、法律諮詢),長期擴展至台聯大、全國。

**核心範圍決策(定案,不要重新討論):** 僅限校內研究媒合。明確排除企業徵才/求職、付費訂閱、廣告、一般民眾註冊、公開問答牆、積分排行榜。五類申請管道:TA(課程助教)、DEPT(系辦短期)、UR(大專生計畫)、REC(推薦信)、IND(產學跨域)。

---

## 二、產出文件清單(架構/治理/研究層面,非程式碼)

以下文件已產出,存放於 `/mnt/user-data/outputs/`(對話環境本機路徑,非 repo 內):

| 文件 | 內容 |
| :-- | :-- |
| `NCCU研究媒合平台_架構書.md` | 完整技術與制度架構(受眾、五類申請、資料分級、生命週期、資安、檢舉機制、多方案並列爭議點) |
| `NCCU研究媒合平台_提案書.md` | 對校方的說服文件(問題陳述、治理與人力組織、經費紀律、需要校方的資源) |
| `政大研究媒合平台_簡介版.md` / `_架構簡報.md` | 給人文社科背景教授看的精簡/專業版(已依回饋反覆修正,最終版不含 MVP 敘事、不含治理/畢業等離題內容,純聚焦架構) |
| `NCCU研究媒合平台_技術規格.md` | 給開發模型(如 Codex)使用的合併版技術規格,含已移除模組清單、開發優先順序 |
| `研究媒合平台_UIUX介面設計分析.md` | 兩個參考介面模式的拆解(依使用者要求,全文不指名平台)+ 延伸研究的 14 項設計方向決策表 |
| `教授需求調查問卷.md` | 28 題問卷,含隱私設計、政大 13 學院 77 系所完整下拉選單(已依官方教學單位頁面查證校正)、Google Forms 串接 Google Sheets 教學 |
| `團隊協作與上手指南.md` | 給發起人的 GitHub 設定步驟 + 給隊友(GDC/Open NCCU/Fintech Club)的上手教學 + 分工權限建議 |
| `資安架構實作規格_給Sonnet執行.md` | 對照 OWASP Top 10:2025 的完整資安實作規格(本文件第五節是其執行進度) |

**重要:** 上述文件是「規劃層」產出,程式碼是「實作層」。兩者已盡量對齊(如問卷的系所清單已同步進資料庫種子資料),但若發現不一致,**以程式碼實際行為為準**,因為程式碼有經過建置驗證,文件可能未即時更新。

---

## 三、程式碼專案現況(`yanlian/` 資料夾)

### 3.1 技術棧與基本資訊

- **框架:** Next.js 14.2.35(App Router)+ TypeScript + Drizzle ORM + SQLite(開發)
- **換 PostgreSQL 的方法:** 見 `README.md` 與 `src/server/db/schema.ts` 檔頭註解,改動範圍收斂在 `src/server/db/` 與 `src/server/repositories/`
- **git:** 已初始化,main 分支,commit 歷史完整保留開發脈絡,尚未推上任何遠端(需使用者自行在 GitHub 開 repo 並 push,AI 無法代為操作,需帳號授權)
- **啟動方式:** `npm install` → `npm run db:migrate` → `npm run db:seed` → `npm run dev`
- **測試帳號:** `admin@nccu.edu.tw`(管理員)、`professor.demo@nccu.edu.tw`(已認領帳號的示範教授,可測身分切換/教授儀表板)、任意 `@g.nccu.edu.tw` 信箱(一般學生)。開發模式驗證碼直接顯示於登入頁畫面。

### 3.2 已完成功能(全部通過 `npm run build`,多數有實際端到端驗證,非僅編譯通過)

**核心媒合功能:**
- 依學院→系所→研究領域→子領域→教授逐層瀏覽(13 學院、77 系所,依政大官方教學單位頁面查證校正;僅英文系有完整領域/子領域示範資料)
- 教授檔案(專長標籤、開放需求、媒合開關)
- 五類需求發布與結構化三步驟申請表單(背景興趣→時間技能→確認送出)
- 教授端並排比較申請 + 狀態機操作(邀請面試/通過/婉拒)
- 檢舉機制(48hr/7 日分級時限)+ 管理後台審核教授/結案檢舉
- 條款簽署存證(含雜湊鏈,見下方資安章節)

**UI/UX 決策落地(對應「研究媒合平台_UIUX介面設計分析.md」決策表):**
- #1 教授列表格狀卡片(資訊分層)
- #2 需求列表即時篩選(前端純篩選,零網路來回)
- #4 檢舉結案雙方中性通知(不做無聲移除)
- #5 三步驟結構化申請表單
- #6 教授端並排比較申請頁
- #9 身分視角切換(學生/教授雙儀表板,僅影響導覽顯示)
- #10 教授新手引導清單(完成度進度條)
- #11 「我的申請」「我的檢舉」進度可視化狀態列
- #12 首頁「為你精選」(非個人化過渡版,已誠實標註)
- 明確**不採用**:教授即時上下線圓點、催促搶案設計、點數購買報價權(與既定原則衝突)

**通知系統:** 新申請、狀態更新、檢舉結案、教授審核結果皆會觸發站內通知,導覽列鈴鐺顯示未讀數。

### 3.3 資安實作現況(對照 OWASP Top 10:2025,詳細規格見「資安架構實作規格_給Sonnet執行.md」)

**已完成並驗證(§2、§3、§4、§5、§7):**

| 項目 | 檔案 | 驗證方式 |
| :-- | :-- | :-- |
| 集中式授權守則,修正申請人可自行變更申請狀態的邏輯漏洞 | `src/server/authz.ts` | 程式邏輯審查 |
| 登入速率限制(同信箱/IP)+ 連續 5 次錯誤鎖定 15 分鐘 + 階梯式延遲 | `src/server/repositories/ratelimit.ts` | 實際打 6 次錯誤驗證碼,第 6 次確認被鎖 |
| Session 輪替(每次登入核發新 token)+ 閒置 7 天逾時 | `src/server/auth.ts` | 程式邏輯審查 |
| 管理員強制 TOTP 雙因素驗證 + 30 分鐘敏感操作重驗 | `src/server/totp.ts`、`/admin/setup-2fa`、`/admin/step-up` | 實際登入 admin 帳號確認被導向 2FA 設定頁 |
| 應用層列舉偵測(10 分鐘瀏覽 40/80 個不同教授觸發軟/硬門檻) | `src/server/repositories/access.ts`、`src/server/anti-scrape.ts` | 實際連續存取 45 個教授頁,確認觸發 6 筆 `enum.detected` 事件 |
| 分級內容曝光(教授簡介、需求全文需登入才顯示完整版) | 各資源頁面 | 實際以未登入狀態存取,確認顯示遮蔽訊息 |
| 條件式人機驗證(可插拔,現用本機挑戰,已預留 Turnstile 正式接點) | `src/server/captcha.ts`、`/verify-human` | 程式邏輯審查 |
| 蜜罐連結(視覺隱藏,爬蟲會讀到) | `.bait-link` CSS class、`/directory-index` | 實際存取後確認寫入 `security_events`(severity=high) |
| 全站安全標頭 + CSP | `next.config.mjs` | 實際 curl -I 確認 CSP/HSTS/X-Frame-Options 皆存在 |
| 統一錯誤邊界(不外洩堆疊/SQL細節) | `src/app/error.tsx`、`global-error.tsx` | 程式邏輯審查 |
| 集中 zod 驗證,狀態值白名單化 | `src/server/schemas.ts` | 程式邏輯審查 |
| AES-256-GCM 欄位加密工具 | `src/server/crypto.ts` | 程式邏輯審查(尚未有實際欄位使用,見下方待辦) |
| 稽核日誌 + 條款簽署雜湊鏈,竄改即斷鏈 | `src/server/repositories/audit.ts`(`audit()`、`verifyAuditChain()`)、`src/server/auth.ts`(`signTerms()`) | **實際竄改測試**:手動改一筆稽核紀錄內容,執行 `verifyAuditChain()` 確認精準指出斷鏈的那一筆 id |
| 安全事件記錄 + high 嚴重度即時告警 | `src/server/repositories/security.ts`、`/admin/security` | 程式邏輯審查 + 實際觸發蜜罐確認事件寫入 |
| Dependabot 週期掃描 + CI(npm audit 未過即擋) | `.github/dependabot.yml`、`.github/workflows/ci.yml` | 已建立,尚未實際跑過(需推上 GitHub 才會觸發) |

**已知殘留漏洞(誠實列管,不要假裝已解決):**
- `drizzle-orm` 目前版本(0.33.0)有一項 SQL injection CVE(需升級至 0.45.2,屬 semver-major 跳版,非破壞性修復無法處理)。**已查證**:目前程式碼所有查詢皆透過參數化的查詢建構器(`db.select().where(eq(...))` 等),沒有任何原始 SQL 拼接或動態識別碼,不會觸及該 CVE 的實際攻擊面。但仍建議日後排定專門的升級+回歸測試時段處理,不應無限期擱置。

**規格中明確未完成的部分(§2.5 部分、§6 全部):**
- **雙人核可機制:** `dual_approvals` 資料表已建立(schema.ts),但**沒有任何邏輯呼叫它**——目前調閱敏感稽核資料只需要單一管理員的 2FA 通過,尚未做到規格要求的「第二位管理員會簽」。這是刻意先擱置、明確告知的缺口,不是遺漏後才發現。
- **檔案上傳安全(§6):** 平台本身尚未開發任何檔案上傳功能(履歷、研究草稿等),因此本節規格內容純屬「未來實作此功能時應遵循的規範」,目前無對應程式碼,這是正確狀態而非缺失。
- **敏感欄位靜態加密(§5.1)實際應用:** `crypto.ts` 加密工具已就緒,但**目前資料庫裡沒有任何欄位真的呼叫它加密**——因為現階段沒有「站外聯絡方式」這類 L2 敏感欄位的實際儲存(規劃中但未實作,見下方「尚未開發的功能」)。

### 3.4 資料庫 schema 現況

除原有的身分/分類/需求/存證表之外,這次資安工程新增了六張表:`login_attempts`(速率限制)、`access_events`(列舉偵測)、`security_events`(安全事件)、`human_checks`(人機驗證通過紀錄)、`dual_approvals`(雙人核可,**表已建但邏輯未實作**)。`users` 表加了 `totp_secret_enc`/`totp_enabled`,`sessions` 加了 `last_used_at`/`step_up_at`,`audit_logs`/`agreement_logs` 加了 `prev_hash`/`hash`。

完整定義見 `src/server/db/schema.ts`,每張表都有中文註解說明用途與對應規格章節。

### 3.5 尚未開發的功能(架構書已規劃,程式碼還沒有)

站內訊息(含頻率上限)、分層揭露的實際站外聯絡方式欄位、帳號生命週期自動化(校友轉換/緩衝期/媒合案凍結)、面試時段預約 + .ics 行事曆同步、教授實驗室群組功能、AI 輔助(研究標籤建議/摘要)、學期聚合統計報告 dashboard、雙人核可邏輯(§2.5)、檔案上傳(§6)。

---

## 四、關鍵決策速查(避免重新討論已定案的事)

| 決策點 | 結論 | 不要做的事 |
| :-- | :-- | :-- |
| 帳號 ID | 系統自動產生、不具業務意義(cuid/uuid) | 不要改用流水號自增 ID(會洩漏規模、擴校要重寫、也破壞反爬取的地基) |
| 內容審核 | AI 只能「隱藏待審」,懲罰須人工 | 不要讓任何自動化機制直接封鎖/懲罰帳號 |
| 資料保存 | 停用與刪除分開規範,L3 存證保留 ≥5 年 | 不要把「停用帳號」實作成立即刪除資料 |
| 治理所有權 | 發起人保留產品決策,營運分階段制度化 | 不要動用學生自治組織預算(公款圖利爭議) |
| 條款文字 | 目前是草稿,`src/server/terms.ts` 內已標註「未經法律審閱」 | 不要當作正式條款使用或移除警語 |
| 反爬取設計哲學 | 應用層行為偵測 > 網路層 IP 過濾(住宅代理可繞過 IP 層) | 不要只做 IP 限流就以為足夠 |
| 人機驗證 | 條件式觸發,不全站強制;不用會餵資料給廣告商的方案 | 不要加全站強制 CAPTCHA |

---

## 五、如果你是接手的 AI,下一步該做什麼

1. **先確認使用者實際想要什麼**,不要預設要接續某個特定待辦——上面「未完成」清單只是誠實揭露現況,不是任務佇列。
2. 若要繼續資安工程:優先順序是雙人核可邏輯(§2.5,表已建、只缺邏輯,相對快)→ drizzle-orm 升級(需要跑一輪回歸測試)。
3. 若要開發新功能:先讀對應的架構書章節(`NCCU研究媒合平台_架構書.md`),不要自己重新設計已經定案的部分。
4. **修改資料庫 schema 後,固定流程是:** `npx drizzle-kit generate` → `npx drizzle-kit migrate` → 若需要重灌示範資料則 `npm run db:seed`(或 `npm run db:reset` 一次做完)→ `npm run build` 確認沒壞 → 若涉及安全機制,實際跑一次功能驗證(不要只信編譯通過)。
5. **不要重讀所有先前產出的文件**——除非使用者明確要求,否則本文件第二、三、四節的摘要已經足夠理解現況,重讀全部文件是不必要的算力浪費(使用者已明確要求過這件事)。
6. 專案尚未推上 GitHub。若使用者要推送,操作步驟已寫在「團隊協作與上手指南.md」,AI 無法代為執行(需使用者本人的帳號授權)。
