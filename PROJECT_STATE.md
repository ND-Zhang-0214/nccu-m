# PROJECT_STATE.md — 政大研究媒合平台專案狀態總覽

> **這份文件的用途:** 讓任何人(或任何一個新開的 AI 對話)在不重讀整個對話歷史的情況下,完整理解這個專案目前做到哪裡、為什麼這樣設計、還缺什麼。結構刻意做成人類跟 AI 都好讀——標題分明、決策都附理由、現況與待辦明確分開,不夾雜過程敘事。
>
> **給接手的 AI 模型看的第一句話:** 這是一個真實在開發中的專案,不是範例或練習。所有「已完成」項目都經過實際建置(`npm run build`)與功能驗證,不是只有程式碼存在。所有「未完成」項目都明確列出,不要假設它們已經做了。修改前先讀對應章節,不要整個專案重新設計。
>
> **最後更新:** 對應 git commit `e16a1af`(面試時段+ics/群組/AI/學期報告 那次)之後的獨立複查。全部 16 個 commit 涵蓋的功能本次都已重新驗證過(不是只信 commit 訊息的宣稱),詳見第三節與 `SECURITY_REVIEW.md`、`FUNCTIONAL_TEST_CHECKLIST.md`。

---

## 一、專案是什麼

**一句話:** 政大校內限定的研究媒合基礎設施——讓教授的研究助理/大專生計畫/推薦信/產學合作等需求,與學生在有身分驗證、有隱私保護、有存證機制的環境中對接。不是求職網站,不是社群平台,不收費、不放廣告。

**發起人背景:** 政大三年級學生(英文系+全球經濟治理雙主修),有學生自治經驗與 TWSE ESG 審查實務經驗。目標是先做出雛型,爭取校方資源(SSO 對接、學籍系統介接、校內主機、法律諮詢),長期擴展至台聯大、全國。

**核心範圍決策(定案,不要重新討論):** 僅限校內研究媒合。明確排除企業徵才/求職、付費訂閱、廣告、一般民眾註冊、公開問答牆、積分排行榜。五類申請管道:TA(課程助教)、DEPT(系辦短期)、UR(大專生計畫)、REC(推薦信)、IND(產學跨域)。

---

## 二、產出文件清單(架構/治理/研究層面,非程式碼)

以下文件已產出,存放於 `/mnt/user-data/outputs/`(對話環境本機路徑,非 repo 內):

| 文件 | 內容 |
| :-- | :-- |
| `NCCU研究媒合平台_架構書.md` | 完整技術與制度架構(受眾、五類申請、資料分級、生命週期、資安、檢舉機制、多方案並列爭議點) |
| `NCCU研究媒合平台_提案書.md` | 對校方的說服文件(問題陳述、治理與人力組織、經費紀律、需要校方的資源) |
| `政大研究媒合平台_簡介版.md` / `_架構簡報.md` | 給人文社科背景教授看的精簡/專業版 |
| `NCCU研究媒合平台_技術規格.md` | 給開發模型使用的合併版技術規格 |
| `研究媒合平台_UIUX介面設計分析.md` | 兩個參考介面模式的拆解 + 14 項設計方向決策表 |
| `教授需求調查問卷.md` | 28 題問卷,含隱私設計、政大 13 學院 77 系所完整下拉選單 |
| `團隊協作與上手指南.md` | 給發起人的 GitHub 設定步驟 + 隊友上手教學 + 分工權限建議 |
| `資安架構實作規格_給Sonnet執行.md` | 對照 OWASP Top 10:2025 的完整資安實作規格原始需求 |

**重要:** 上述文件是「規劃層」產出,程式碼是「實作層」。若發現不一致,**以程式碼實際行為為準**。

---

## 三、程式碼專案現況(`yanlian/` 資料夾)

### 3.1 技術棧與基本資訊

- **框架:** Next.js 14.2.35(App Router)+ TypeScript + Drizzle ORM 0.45.2 + SQLite(開發)
- **換 PostgreSQL 的方法:** 見 `README.md` 與 `src/server/db/schema.ts` 檔頭註解,改動範圍收斂在 `src/server/db/` 與 `src/server/repositories/`(未實測換庫過程本身)
- **git:** 已初始化,main 分支,16 個 commit,尚未推上任何遠端(需使用者自行在 GitHub 開 repo 並 push)
- **啟動方式:** `npm install` → `npm run db:migrate` → `npm run db:seed` → `npm run dev`
- **測試帳號:** `admin@nccu.edu.tw`(管理員)、`professor.demo@nccu.edu.tw`(已認領帳號的示範教授)、任意 `@g.nccu.edu.tw` 信箱(一般學生)。開發模式驗證碼直接顯示於登入頁畫面。

### 3.2 已完成功能總覽(全部通過 `npm run build`,34 個路由,型別檢查全過)

**核心媒合功能:** 目錄瀏覽、教授檔案、五類需求發布與結構化申請、教授端並排比較申請+狀態機、檢舉機制、條款簽署存證、通知系統。

**M4 站內訊息:** 對話(學生↔教授僅能透過已存在申請開啟,教授↔教授可直接發起)、忙碌/有空狀態雙方手動設定、每日新對話頻率上限、§5.1 聯絡方式加密(`user_contacts`)+ 對話內主動揭露留痕(`contact_disclosures`)。

**§2.5 雙人核可:** 管理員 A 申請敏感調閱(如查看訊息內容)→ 另一位管理員 B 核可(不可自核)→ A 於核可後 30 分鐘時效窗口內可實際查看,逾窗口需重新申請。全程稽核留痕。

**§6 檔案上傳:** 白名單型別以 magic number 判斷(非信任副檔名/Content-Type)、10MB 大小上限(宣告值+實際 buffer 雙重檢查)、掃描介面(`scan.ts`)、每帳號每小時 20 次配額與寫入包在同一原子交易(高併發下不可繞過,見下方「已修正的漏洞」)。

**帳號生命週期自動化:** 畢業偵測(管理員手動觸發)→ 6 個月緩衝期(期間權限不受影響)→ 到期自動轉 ALUM(唯讀);休學(SUSPENDED)/復學/退學封存(ARCHIVED);教授帳號交接(30–90 天前申請,自動通知利益相關人,到期自動關閉開放需求,凍結非刪除);帳號血緣(學士→碩士等身分轉換關聯,舊帳號資料不動)。

**M7 面試時段 + ics:** 時段建立、原子條件更新防併發搶單、地點僅預約後揭露、ics 訂閱 token(150 天到期)、行事曆 feed 產出。

**M8 教授群組:** 群組貼文一律不公開(無公開牆、無跨群組轉發),依信箱邀請已存在帳號,成員/外人權限隔離。

**M9 AI 輔助:** 研究標籤建議(教授簡介→候選子領域)、申請動機摘要。有 `ANTHROPIC_API_KEY` 時真呼叫 Claude API,無金鑰時退回關鍵字比對啟發式演算法(兩條路徑都驗證過,退回路徑不是裝飾)。教授必須手動確認才會實際套用建議,絕不自動寫入。

**學期聚合報告 dashboard:** 新使用者數、需求分類統計、申請狀態分布、媒合成功率、檢舉分布與平均處理時效、生命週期事件統計。**分組樣本 <5 一律隱藏**(精準邊界測試過:4 筆隱藏、第 5 筆起顯示)。

### 3.3 資安實作現況(對照 OWASP Top 10:2025)

**完整逐項複查結果、驗證方法與證據見獨立文件 `SECURITY_REVIEW.md`**,不在此重複。摘要結論:核心防護(集中授權、加密、注入防護、安全標頭、身分驗證、完整性、監控)全數已處理且本次複查重新實測過;已知殘留風險(如反爬取 actorKey 對訪客用 IP+UA 雜湊、生命週期批次靠手動觸發非真排程器)已誠實列管於該文件第三節,附上線前必辦清單。

**已修正的真實漏洞(非規劃階段就避免,是負載測試時真的抓到的):**
檔案上傳配額檢查與寫入原本分成兩步(先查詢目前用量、再寫入),高併發下會被繞過(N 個併發請求同時查到「還沒超額」,一起寫入超過配額)。修正為 `createAttachmentIfUnderQuota` 單一原子交易。本次複查用 25 個併發請求重新打過一次 20 次/小時的配額,資料庫實際筆數與磁碟檔案數精準等於 20,確認修正有效且沒有回歸。

### 3.4 資料庫 schema 現況

完整定義見 `src/server/db/schema.ts`,每張表皆有中文註解。目前共 27 張表,涵蓋身分/分類/需求/存證(原有)、資安(§2-5 speedwork:login_attempts/access_events/security_events/human_checks/dual_approvals)、訊息(conversations/conversation_members/messages/user_contacts/contact_disclosures)、檔案(attachments)、生命週期(account_lineage/professor_relinquishments,users 表補充 lifecycleBufferEndsAt/lifecycleNote 欄位)、面試(interview_slots/ics_tokens)、群組(groups/group_members/group_posts)、AI 留痕(如適用)。

### 3.5 已知範圍界定(誠實列出,不是遺漏)

- 「偵測離校」與「批次到期處理」目前靠管理員手動觸發,不是真的接教務處系統或排程器——邏輯本身真實運作,只是觸發方式在此開發環境下是手動的。正式上線需接真實系統,詳見 `SECURITY_REVIEW.md` 第五節必辦清單。
- 首頁「為你精選」是非個人化過渡版本(依最近開放需求輪替取樣)。
- 唯讀帳號(ALUM/SUSPENDED/ARCHIVED)的寫入防護,目前是在 `applyAction`/`sendMessageAction`/`createGroupPostAction` 等處各自寫狀態檢查,沒有統一收斂成單一守則呼叫——每處都有檢查(逐一確認過),但屬程式碼一致性瑕疵,建議下次維護時處理。
- 換 PostgreSQL 的路徑在 schema 設計上已考量,但未實際執行測試(此環境無 PostgreSQL 可測)。

---

## 四、關鍵決策速查(避免重新討論已定案的事)

| 決策點 | 結論 | 不要做的事 |
| :-- | :-- | :-- |
| 帳號 ID | 系統自動產生、不具業務意義(uuid) | 不要改用流水號自增 ID |
| 內容審核 | AI 只能「隱藏待審」,懲罰須人工 | 不要讓任何自動化機制直接封鎖/懲罰帳號 |
| 資料保存 | 停用與刪除分開規範,L3 存證保留 ≥5 年 | 不要把「停用帳號」實作成立即刪除資料 |
| 治理所有權 | 發起人保留產品決策,營運分階段制度化 | 不要動用學生自治組織預算 |
| 條款文字 | 目前是草稿,`src/server/terms.ts` 內已標註「未經法律審閱」 | 不要當作正式條款使用或移除警語 |
| 反爬取設計哲學 | 應用層行為偵測 > 網路層 IP 過濾 | 不要只做 IP 限流就以為足夠 |
| 人機驗證 | 條件式觸發,不全站強制 | 不要加全站強制 CAPTCHA |
| 站內訊息 | 學生→教授僅能透過結構化申請開啟;教授↔教授可直接發起 | 不要讓學生任意私訊陌生教授 |
| 聯絡方式揭露 | 加密存放,對話中雙方主動揭露才可見,揭露即留痕 | 不要預設公開聯絡方式,或讓揭露無留痕 |
| 群組定位 | 貼文一律不公開,維持研究媒合工具定位 | 不要加公開牆、跨群組轉發、社群化功能 |
| AI 輔助定位 | 只產生建議,教授必須手動確認才套用 | 不要讓 AI 建議自動寫入資料庫 |
| 學期報告 | 分組樣本 <5 一律隱藏數字 | 不要為了「資料好看」而降低隱藏門檻 |

---

## 五、如果你是接手的 AI,下一步該做什麼

1. **先確認使用者實際想要什麼**,不要預設要接續某個特定待辦。
2. 若要處理殘留風險:優先順序見 `SECURITY_REVIEW.md` 第五節「正式上線前必辦清單」,建議從「統一唯讀帳號檢查」與「獨立 ENCRYPTION_KEY」這兩項風險/工作量比最高的先做。
3. 若要開發新功能:先讀對應的架構書章節,不要自己重新設計已經定案的部分(見第四節速查表)。
4. **修改資料庫 schema 後,固定流程是:** `npx drizzle-kit generate` → `npx drizzle-kit migrate` → `npm run db:seed`(或 `npm run db:reset` 一次做完)→ `npm run build` 確認沒壞 → 若涉及安全或併發邏輯,實際寫測試腳本跑一次驗證(不要只信編譯通過,也不要只信之前的 commit 訊息宣稱——本輪複查就抓到測試腳本自己的一個錯,說明「重新獨立驗證」是必要的習慣,不是多餘的重複工作)。
5. 測試腳本(`*-test.ts`)用完即刪除,不要留在版本庫或提交進 git——這是本專案的既定慣例,保持 repo 乾淨。
6. **不要重讀所有先前產出的文件**——本文件第二、三、四節的摘要已經足夠理解現況。
7. 專案尚未推上 GitHub。若使用者要推送,操作步驟見「團隊協作與上手指南.md」,AI 無法代為執行(需使用者本人的帳號授權)。
8. 完整功能測試單見 `FUNCTIONAL_TEST_CHECKLIST.md`——區分「本輪已自動化驗證」與「建議人工瀏覽器複查」兩類,兩者都要看,不要只看其中一種。
