# 資安設計複查報告

**複查日期:** 2026-07-21
**複查方式:** 逐項程式碼審查 + 實際執行對抗性/併發/邊界測試(非僅閱讀程式碼判斷),
測試腳本執行後即刪除,不留在版本庫中;本文件記錄的是複查**結果**與**證據**,不是測試腳本本身。
**範圍:** 本次複查涵蓋全部 16 個 commit 的完整程式碼,不只本輪新增的四項功能。

---

## 一、總結判斷

**核心防護到位,可視為「校內小規模試營運」等級的資安基礎。** 沒有發現嚴重(critical)等級的漏洞。
發現並修正過 1 個真實漏洞(檔案上傳配額競態條件,見 §7),本次複查另外抓到 1 個**測試腳本**本身的
錯誤(不是產品漏洞,見附錄)。仍有數項**已知、已誠實標註**的簡化與殘留風險,詳列於第三節,
正式上線前需要處理。

---

## 二、對照 OWASP Top 10:2025,逐項複查結果

| # | 類別 | 狀態 | verification 證據 |
|---|---|---|---|
| A01 | 權限控管失效(Broken Access Control) | ✅ 已處理 | 集中式 `authz.ts`,不分散寫判斷。實測:①非群組成員存取群組頁,真實 session 下 HTTP 307 導回,且寫入 `security_events`(severity=high);②`requireApplicationStatusEditor` 刻意排除申請人本人,防止學生自行核准/婉拒自己的申請(邏輯層已修正過的漏洞);③附件/對話/時段預約皆有 IDOR 防護(只能操作自己的申請關聯資源),bookSlotAction 明確檢查 `app.applicantId !== user.id` |
| A02 | 加密機制失效 | ✅ 已處理,有已知簡化 | AES-256-GCM 實測加密/解密往返正確、格式錯誤 fail-closed 拋錯。**已知簡化:** 開發環境無 `ENCRYPTION_KEY` 時會從 `SESSION_SECRET` 派生金鑰,正式環境**必須**設定獨立金鑰,README 已列入上線清單,但這是「人為必須記得做」而非「系統強制」,屬於殘留風險 |
| A03 | 注入攻擊(Injection) | ✅ 已處理 | 全站透過 Drizzle ORM 參數化查詢,無任何字串拼接 SQL。drizzle-orm 已由 0.33 升級至 0.45.2(解除舊版已知 CVE),本次複查重新跑過 `drizzle-kit generate` 確認無 schema 差異、build 全過 |
| A04 | 不安全設計 | ✅ 已處理 | 學期報告分組樣本 <5 一律隱藏(實測精準邊界:4 筆隱藏、第 5 筆起顯示);聯絡方式分層揭露(未揭露前不進資料庫查詢路徑之外的地方出現明文);面試地點在預約前不回傳(`listOpenSlotsForPosting` 實測回傳物件不含 `location` 欄位) |
| A05 | 安全設定缺陷 | ✅ 已處理 | CSP、HSTS、X-Content-Type-Options、X-Frame-Options、Referrer-Policy、Permissions-Policy 六項標頭本次複查**實際起 server 用 curl -I 覆核過**,非僅檢查設定檔存在。狀態值一律白名單化(zod enum),不接受任意字串 |
| A06 | 易受攻擊或過時的元件 | ⚠️ 部分殘留 | `npm audit` 殘留 6 項,**全數為 devDependencies**(drizzle-kit 內部依賴的 esbuild、postcss),不進 production bundle。next 已釘死在 14.2.x 系列最新修補版(14.2.35)。殘留項屬低風險但建議下次工具鏈更新時一併處理 |
| A07 | 身分驗證與識別失效 | ✅ 已處理 | Email 驗證碼登入(僅允許 `@nccu.edu.tw` / `@g.nccu.edu.tw`),§2.3 三層速率限制(email 60秒/10分鐘、IP 10分鐘)+ 連續錯誤 5 次鎖定 15 分鐘,皆為實際查資料庫計數,非僅檢查程式碼存在。Session:token 只存雜湊,絕對逾時 14 天+閒置逾時 7 天雙重機制,登入時輪替 session。管理員強制 TOTP 2FA + 敏感操作 30 分鐘重驗窗口(step-up) |
| A08 | 軟體與資料完整性失效 | ✅ 已處理 | 稽核紀錄與條款簽署皆採雜湊鏈(`hash = sha256(前一筆hash + 本筆內容)`)。本次複查**實際竄改一筆稽核紀錄**再跑 `verifyAuditChain()`,精準指出斷鏈的那一筆 id,竄改後還原確認鏈可修復偵測邏輯本身不受影響 |
| A09 | 安全記錄與監控失效 | ✅ 已處理 | `security_events`(反爬取/授權拒絕/鎖定等資安事件)與 `audit_logs`(業務操作)分離記錄,職責清楚。應用層列舉偵測:10 分鐘內存取 ≥40 個不同教授頁觸發 soft(漸進延遲)、≥80 觸發 hard(強制人機驗證),本次複查用合成資料實測觸發正確,且**正常瀏覽(3個不同頁)確認不誤觸發** |
| A10 | 伺服端請求偽造(SSRF) | ✅ 不適用/已規避 | 系統不含任何「依使用者輸入的 URL 發出伺服端請求」的功能(AI 呼叫的是寫死的 Anthropic API 端點,非使用者可控 URL) |

---

## 三、已知殘留風險與限制(誠實列出,非隱藏)

這些不是被忽略的漏洞,是**已經在程式碼註解或 README 裡明確標註過的範圍界定**,複查時逐一確認
標註屬實、且沒有被誇大或淡化:

1. **反爬取的 actorKey 對未登入訪客用 IP+UA 雜湊**——同一 IP 的多個使用者(如系辦公用電腦)
   會共用一把 key。程式碼註解已誠實標註此限制,升級路徑是換成 middleware 派發的匿名 cookie,
   本輪未實作(非本輪範圍)。
2. **帳號生命週期的「偵測離校」是管理員手動觸發**,不是真的接教務處學籍系統或信箱退信偵測——
   `lifecycle.ts` 檔頭註解已誠實說明這是因為開發環境沒有真實系統可接,但「緩衝期到期後自動轉換」
   這條邏輯本身是真實運作的(本次複查已重新驗證,見測試單)。
3. **批次到期處理(校友轉換/教授交接)靠管理員手動按鈕觸發**,非真正的排程器(cron)。同上,
   函式邏輯本身真實,只是觸發方式在此環境下非自動定時,正式上線需接排程基礎設施。
4. **首頁「為你精選」是非個人化過渡版本**(依最近開放需求輪替取樣),README 已標註,非真正
   個人化推薦,不影響資安面,列在此處是為了維持文件的誠實完整性。
5. **AI 輔助的 Anthropic API 金鑰為選配**,未設定時退回關鍵字比對啟發式演算法。本次複查**兩條
   路徑都實測過**(標籤建議產生結果且不發明候選外標籤、摘要非空且短於原文、空輸入不產生幻覺內容)。
6. **唯讀帳號(ALUM/SUSPENDED/ARCHIVED)寫入防護沒有統一走同一支 `requireActiveUser()`**——
   本次複查發現 `applyAction`、`sendMessageAction`、`createGroupPostAction` 是各自寫
   `if (user.status !== "ACTIVE")` 而非呼叫共用函式,雖然每處都確實檢查了(逐一確認過),
   但屬於程式碼一致性瑕疵,增加日後漏改一處的風險。**建議下次維護時收斂成單一守則**。
7. **開發環境 `dev.db` 為單檔 SQLite**,正式環境須依 README/schema.ts 檔頭指南換 PostgreSQL,
   換的過程本身未經實測(因本環境無 PostgreSQL 可測),schema 設計上已考量欄位型別相容性。

---

## 四、本次複查實際執行的驗證(非僅程式碼閱讀)

| 驗證項目 | 方法 | 結果 |
|---|---|---|
| 面試時段併發搶單 | 10 個學生 `Promise.all` 併發呼叫 `bookSlot` | 精準 1 個成功,資料庫覆核一致 |
| 檔案上傳併發配額 | 25 個併發 HTTP 上傳打 20 次/小時配額 | 精準 20 成功、5 被拒(429),磁碟檔案數與資料庫筆數一致(無孤兒檔案) |
| 檔案上傳對抗性測試 | 偽造副檔名(exe偽裝pdf)、路徑穿越檔名、超大檔案、未登入 | 415 / 儲存為安全隨機檔名(未越權)/ 413 / 401,四項皆正確擋下 |
| 稽核鏈竄改偵測 | 直接竄改資料庫一筆稽核紀錄,重跑驗證函式 | 精準指出斷鏈那一筆 id |
| 雙人核可機制 | 自己核可自己、B 核可 A 的申請、逾時後重新申請 | 自核被拒、跨人核可成功、時效窗口正確 |
| 學期報告 <5 抑制規則邊界 | 精準製造 4 筆與第 5 筆檢舉 | 4 筆隱藏,滿 5 筆後顯示實際數字 |
| 應用層列舉偵測 | 45 個合成不同資源 ID 連續存取 + 3 個正常瀏覽對照組 | 正確觸發 soft 風險等級,正常瀏覽不誤觸發 |
| 帳號生命週期批次轉換 | 手動將緩衝期改到過去時間戳,重跑批次函式 | 正確轉為 ALUM,交接到期正確關閉開放需求 |
| CSP 等安全標頭 | 實際啟動 server,`curl -I` 覆核回應標頭 | 六項標頭全部確認生效 |
| 訊息+聯絡方式加密 | 加密後查資料庫原始值、揭露後解密比對 | 原始值不含明碼,揭露後對方端解密正確 |

**附錄——本次複查過程中的一個誠實記錄:** 應用層列舉偵測第一次測試回傳 `risk=none`(預期應觸發),
追查後確認是**測試腳本本身的錯**——用種子資料裡的教授筆數(只有 5 位)去湊「45 次存取」,
根本湊不到 40 的門檻,不是系統邏輯有問題。換成合成的 45 個不同資源 ID 後,偵測邏輯正確觸發。
這裡如實記錄,是因為值得說明「測試沒過」不代表「系統有洞」,兩者需要分開判斷,不能省略追查步驟。

---

## 五、正式上線前的必辦清單(彙整自本文件與 README)

- [ ] 設定獨立的 `ENCRYPTION_KEY` 環境變數(32 bytes base64),不可依賴 `SESSION_SECRET` 派生的開發降級路徑
- [ ] 接上真實寄信服務(目前開發模式驗證碼直接顯示於畫面)
- [ ] 接上教務處學籍 API 或信箱退信機制,取代目前的管理員手動觸發畢業偵測
- [ ] 接上排程器(cron/GitHub Actions/Vercel Cron)定期呼叫 `processLifecycleTransitions()`
- [ ] 換 PostgreSQL(依 `schema.ts` 檔頭指南),並實際執行一次遷移驗證
- [ ] 未登入訪客反爬取 key 由 IP+UA 雜湊升級為 middleware 派發的匿名 cookie
- [ ] 收斂 `applyAction`/`sendMessageAction`/`createGroupPostAction` 的唯讀帳號檢查為統一呼叫 `requireActiveUser()`
- [ ] 若要接 Cloudflare Turnstile(§3.4 人機驗證),設定對應環境變數並移除目前的開發模式繞過
- [ ] `npm audit` 殘留的 devDependencies 漏洞列入下次工具鏈更新
